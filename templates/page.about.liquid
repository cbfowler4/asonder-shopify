<!-- /templates/page.liquid -->
<div>    
  <header class="section-header text-center about-header">
    <h1>{{ page.title }}</h1>
    <hr class="hr--small">
  </header>

  <div class='about-main'>
    <div class='about-image'>
      {{ 'team-split-screen_copy.jpg' | file_img_url: '1200x750' | img_tag }}
    </div>

    <div class="about-content">
      {{ page.content }}
    </div>

    <img class="flt-circle" src="{{ 'circle.png' | asset_url }}" />

  </div>

</div>

<style>
  .about-main {
    position: relative;
  }

  .about-image {
    position: relative;
    z-index: 4;
  }

  .flt-circle {
    position: absolute;
    height: 336px;
    opacity: .2;
    top: -112px;
    right: 177px;
    z-index: 0;
  }

  .about-main {
    z-index: 10;
  }
</style>